---
import { Image } from 'astro:assets';
import PageLayout from '../../layouts/PageLayout.astro';
import type { Translations } from '../../i18n/utils';
import heroBg from '../../assets/gallery/about-mandre/about-mandre.jpg';
import mandre1 from '../../assets/gallery/about-mandre/mandre-1.jpg';
import mandre2 from '../../assets/gallery/about-mandre/mandre-2.jpg';
import mandre3 from '../../assets/gallery/about-mandre/mandre-3.jpg';
import mandre4 from '../../assets/gallery/about-mandre/mandre-4.jpg';
import mandre5 from '../../assets/gallery/about-mandre/mandre-5.jpg';
import beaches1 from '../../assets/gallery/about-mandre/beaches-1.jpg';
import beaches2 from '../../assets/gallery/about-mandre/beaches-2.jpg';
import beaches3 from '../../assets/gallery/about-mandre/beaches-3.jpg';
import beaches4 from '../../assets/gallery/about-mandre/beaches-4.jpg';
import beaches5 from '../../assets/gallery/about-mandre/beaches-5.jpg';
import beaches6 from '../../assets/gallery/about-mandre/beaches-6.jpg';
import family1 from '../../assets/gallery/about-mandre/family-1.jpg';
import family2 from '../../assets/gallery/about-mandre/family-2.jpg';
import family3 from '../../assets/gallery/about-mandre/family-3.png';
import family4 from '../../assets/gallery/about-mandre/family-4.png';
import family5 from '../../assets/gallery/about-mandre/family-5.jpg';
import family6 from '../../assets/gallery/about-mandre/family-6.jpg';
import pagCheese from '../../assets/gallery/about-mandre/pag-cheese.jpg';
import pagLamb from '../../assets/gallery/about-mandre/pag-lamb.jpg';
import lifeOnMars1 from '../../assets/gallery/about-mandre/life-on-mars-1.jpg';
import lifeOnMars2 from '../../assets/gallery/about-mandre/life-on-mars-2.jpg';
import boatExcursion1 from '../../assets/gallery/about-mandre/boat-excursion-1.jpg';
import silba from '../../assets/gallery/about-mandre/silba.jpg';
import krka from '../../assets/gallery/about-mandre/krka.jpg';
import plitvice from '../../assets/gallery/about-mandre/plitvice.jpg';

interface Props {
  lang: string;
  T: Translations;
}

const { lang, T } = Astro.props;
const prefix = lang === 'en' ? '' : `/${lang}`;
---

<PageLayout
  title={T.aboutMandre.metaTitle}
  description={T.aboutMandre.metaDescription}
  pageTitle={T.aboutMandre.pageTitle}
  pageSubtitle={T.aboutMandre.pageSubtitle}
  label={T.aboutMandre.label}
  bgClass="ph-town"
  bgImage={heroBg}
  heroObjectPosition="center 54.7%"
  lang={lang}
>

  <!-- ═══ THE TOWN ═══ -->
  <section class="section">
    <div class="container">
      <div class="mandre-grid">
        <div>
          <span class="section-label">{T.aboutMandre.townLabel}</span>
          <h2>{T.aboutMandre.townTitle}</h2>
          <p>{T.aboutMandre.townBody1}</p>
          <p style="margin-top:1rem;">{T.aboutMandre.townBody2}</p>
        </div>
        <div>
          <div class="carousel" id="mandre-carousel">
            <div class="carousel__track">
              <div class="carousel__slide"><Image src={mandre1} alt="Mandre town" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={mandre2} alt="Mandre town" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={mandre3} alt="Mandre town" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={mandre4} alt="Mandre town" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={mandre5} alt="Mandre town" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
            </div>
            <button class="carousel__btn carousel__btn--prev" aria-label="Previous image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 1L1 8L8 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <button class="carousel__btn carousel__btn--next" aria-label="Next image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L8 8L1 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <div class="carousel__dots">
              <button class="carousel__dot carousel__dot--active" aria-label="Slide 1"></button>
              <button class="carousel__dot" aria-label="Slide 2"></button>
              <button class="carousel__dot" aria-label="Slide 3"></button>
              <button class="carousel__dot" aria-label="Slide 4"></button>
              <button class="carousel__dot" aria-label="Slide 5"></button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ BEACHES ═══ -->
  <section class="section" style="background:var(--soft-sky);">
    <div class="container">
      <div class="mandre-grid mandre-grid--reverse">
        <div>
          <div class="carousel" id="beaches-carousel">
            <div class="carousel__track">
              <div class="carousel__slide"><Image src={beaches1} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={beaches2} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={beaches3} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={beaches4} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={beaches5} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={beaches6} alt="Mandre beach" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
            </div>
            <button class="carousel__btn carousel__btn--prev" aria-label="Previous image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 1L1 8L8 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <button class="carousel__btn carousel__btn--next" aria-label="Next image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L8 8L1 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <div class="carousel__dots">
              <button class="carousel__dot carousel__dot--active" aria-label="Slide 1"></button>
              <button class="carousel__dot" aria-label="Slide 2"></button>
              <button class="carousel__dot" aria-label="Slide 3"></button>
              <button class="carousel__dot" aria-label="Slide 4"></button>
              <button class="carousel__dot" aria-label="Slide 5"></button>
              <button class="carousel__dot" aria-label="Slide 6"></button>
            </div>
          </div>
        </div>
        <div>
          <span class="section-label">{T.aboutMandre.beachesLabel}</span>
          <h2>{T.aboutMandre.beachesTitle}</h2>
          <p set:html={T.aboutMandre.beachesBody1}></p>
          <p style="margin-top:1rem;">{T.aboutMandre.beachesBody2}</p>
          <div class="beach-tag">{T.aboutMandre.blueFlagTag}</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ FOR FAMILIES ═══ -->
  <section class="section">
    <div class="container">
      <div class="mandre-grid">
        <div>
          <span class="section-label">{T.aboutMandre.familyLabel}</span>
          <h2>{T.aboutMandre.familyTitle}</h2>
          <p>{T.aboutMandre.familyBody}</p>
          <ul class="mandre-list">
            <li>
              <span class="mandre-list__icon">🛝</span>
              <span set:html={T.aboutMandre.familyBullet1}></span>
            </li>
            <li>
              <span class="mandre-list__icon">⚽</span>
              <span set:html={T.aboutMandre.familyBullet2}></span>
            </li>
            <li>
              <span class="mandre-list__icon">🚲</span>
              <span set:html={T.aboutMandre.familyBullet3}></span>
            </li>
            <li>
              <span class="mandre-list__icon">🎶</span>
              <span set:html={T.aboutMandre.familyBullet4}></span>
            </li>
          </ul>
        </div>
        <div>
          <div class="carousel" id="family-carousel">
            <div class="carousel__track">
              <div class="carousel__slide"><Image src={family1} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={family2} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={family3} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={family4} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={family5} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={family6} alt="Mandre family activities" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
            </div>
            <button class="carousel__btn carousel__btn--prev" aria-label="Previous image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 1L1 8L8 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <button class="carousel__btn carousel__btn--next" aria-label="Next image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L8 8L1 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <div class="carousel__dots">
              <button class="carousel__dot carousel__dot--active" aria-label="Slide 1"></button>
              <button class="carousel__dot" aria-label="Slide 2"></button>
              <button class="carousel__dot" aria-label="Slide 3"></button>
              <button class="carousel__dot" aria-label="Slide 4"></button>
              <button class="carousel__dot" aria-label="Slide 5"></button>
              <button class="carousel__dot" aria-label="Slide 6"></button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ GETTING AROUND ═══ -->
  <section class="section" style="background:var(--warm-sand);">
    <div class="container">
      <div class="text-center" style="margin-bottom:2.5rem;">
        <span class="section-label">{T.aboutMandre.convenienceLabel}</span>
        <h2>{T.aboutMandre.convenienceTitle}</h2>
      </div>
      <div class="convenience-grid">
        <div class="convenience-item">
          <span class="convenience-icon">{T.aboutMandre.convenienceItem1Icon}</span>
          <h4>{T.aboutMandre.convenienceItem1Title}</h4>
          <p>{T.aboutMandre.convenienceItem1Body}</p>
        </div>
        <div class="convenience-item">
          <span class="convenience-icon">{T.aboutMandre.convenienceItem2Icon}</span>
          <h4>{T.aboutMandre.convenienceItem2Title}</h4>
          <p>{T.aboutMandre.convenienceItem2Body}</p>
        </div>
        <div class="convenience-item">
          <span class="convenience-icon">{T.aboutMandre.convenienceItem3Icon}</span>
          <h4>{T.aboutMandre.convenienceItem3Title}</h4>
          <p>{T.aboutMandre.convenienceItem3Body}</p>
        </div>
        <div class="convenience-item">
          <span class="convenience-icon">{T.aboutMandre.convenienceItem4Icon}</span>
          <h4>{T.aboutMandre.convenienceItem4Title}</h4>
          <p>{T.aboutMandre.convenienceItem4Body}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ LOCAL FLAVOURS ═══ -->
  <section class="section">
    <div class="container">
      <div class="mandre-grid">
        <div>
          <span class="section-label">{T.aboutMandre.tasteLabel}</span>
          <h2>{T.aboutMandre.tasteTitle}</h2>
          <p>{T.aboutMandre.tasteBody}</p>
          <div class="food-card">
            <div class="food-card__emoji">🧀</div>
            <div>
              <h4>{T.aboutMandre.cheeseTitle}</h4>
              <p>{T.aboutMandre.cheeseBody}</p>
            </div>
          </div>
          <div class="food-card" style="margin-top:1rem;">
            <div class="food-card__emoji">🍖</div>
            <div>
              <h4>{T.aboutMandre.lambTitle}</h4>
              <p>{T.aboutMandre.lambBody}</p>
            </div>
          </div>
        </div>
        <div>
          <div class="carousel" id="food-carousel">
            <div class="carousel__track">
              <div class="carousel__slide"><Image src={pagCheese} alt="Paški sir — Pag cheese" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide"><Image src={pagLamb} alt="Paška janjetina — Pag lamb" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
            </div>
            <button class="carousel__btn carousel__btn--prev" aria-label="Previous image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 1L1 8L8 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <button class="carousel__btn carousel__btn--next" aria-label="Next image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L8 8L1 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <div class="carousel__dots">
              <button class="carousel__dot carousel__dot--active" aria-label="Slide 1"></button>
              <button class="carousel__dot" aria-label="Slide 2"></button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ OUR PICKS ═══ -->
  <section class="section" style="background:var(--soft-sky);">
    <div class="container">
      <div class="text-center" style="margin-bottom:2.5rem;">
        <span class="section-label">{T.aboutMandre.picksLabel}</span>
        <h2>{T.aboutMandre.picksTitle}</h2>
        <p style="color:var(--text-muted); max-width:560px; margin:0.75rem auto 0;">{T.aboutMandre.picksSub}</p>
      </div>
      <div class="picks-grid">
        <div class="pick-card card">
          <div class="pick-num">01</div>
          <h4>{T.aboutMandre.pick1Title}</h4>
          <p>{T.aboutMandre.pick1Body}</p>
        </div>
        <div class="pick-card card">
          <div class="pick-num">02</div>
          <h4>{T.aboutMandre.pick2Title}</h4>
          <p>{T.aboutMandre.pick2Body}</p>
        </div>
        <div class="pick-card card">
          <div class="pick-num">03</div>
          <h4>{T.aboutMandre.pick3Title}</h4>
          <p>{T.aboutMandre.pick3Body}</p>
        </div>
        <div class="pick-card card">
          <div class="pick-num">04</div>
          <h4>{T.aboutMandre.pick4Title}</h4>
          <p>{T.aboutMandre.pick4Body}</p>
        </div>
        <div class="pick-card card">
          <div class="pick-num">05</div>
          <h4>{T.aboutMandre.pick5Title}</h4>
          <p>{T.aboutMandre.pick5Body}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ DAY TRIPS ═══ -->
  <section class="section">
    <div class="container">
      <div class="mandre-grid">
        <div>
          <span class="section-label">{T.aboutMandre.tripsLabel}</span>
          <h2>{T.aboutMandre.tripsTitle}</h2>
          <p>{T.aboutMandre.tripsBody}</p>
          <ul class="trip-list">
            <li>
              <strong>{T.aboutMandre.trip1Title}</strong>
              <span>{T.aboutMandre.trip1Body}</span>
            </li>
            <li>
              <strong>{T.aboutMandre.trip2Title}</strong>
              <span>{T.aboutMandre.trip2Body}</span>
            </li>
            <li>
              <strong>{T.aboutMandre.trip3Title}</strong>
              <span>{T.aboutMandre.trip3Body}</span>
            </li>
          </ul>
        </div>
        <div>
          <div class="carousel" id="daytrips-carousel">
            <div class="carousel__track">
              <div class="carousel__slide" data-caption="Life on Mars Trail"><Image src={lifeOnMars1} alt="Life on Mars Trail" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide" data-caption="Life on Mars Trail"><Image src={lifeOnMars2} alt="Life on Mars Trail" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide" data-caption="Boat excursion from Luka Mandre"><Image src={boatExcursion1} alt="Boat excursion from Luka Mandre" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide" data-caption="Island of Silba"><Image src={silba} alt="Island of Silba" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide" data-caption="Krka National Park"><Image src={krka} alt="Krka National Park" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
              <div class="carousel__slide" data-caption="Plitvice Lakes National Park"><Image src={plitvice} alt="Plitvice Lakes National Park" widths={[400, 700, 1100]} sizes="(max-width: 900px) 100vw, 50vw" /></div>
            </div>
            <button class="carousel__btn carousel__btn--prev" aria-label="Previous image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M8 1L1 8L8 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <button class="carousel__btn carousel__btn--next" aria-label="Next image">
              <svg width="9" height="16" viewBox="0 0 9 16" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M1 1L8 8L1 15" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </button>
            <div class="carousel__dots">
              <button class="carousel__dot carousel__dot--active" aria-label="Slide 1"></button>
              <button class="carousel__dot" aria-label="Slide 2"></button>
              <button class="carousel__dot" aria-label="Slide 3"></button>
              <button class="carousel__dot" aria-label="Slide 4"></button>
              <button class="carousel__dot" aria-label="Slide 5"></button>
              <button class="carousel__dot" aria-label="Slide 6"></button>
            </div>
            <div class="carousel__caption">Life on Mars trail</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ AROUND THE ISLAND ═══ -->
  <section class="section" style="background:var(--soft-sky);">
    <div class="container">
      <div class="text-center" style="margin-bottom:2.5rem;">
        <span class="section-label">{T.aboutMandre.aroundLabel}</span>
        <h2>{T.aboutMandre.aroundTitle}</h2>
      </div>
      <div class="distances-grid">
        <div class="distance-card">
          <div class="distance-time">{T.aboutMandre.dist1Time}</div>
          <h4>{T.aboutMandre.dist1Title}</h4>
          <p>{T.aboutMandre.dist1Body}</p>
        </div>
        <div class="distance-card">
          <div class="distance-time">{T.aboutMandre.dist2Time}</div>
          <h4>{T.aboutMandre.dist2Title}</h4>
          <p>{T.aboutMandre.dist2Body}</p>
        </div>
        <div class="distance-card">
          <div class="distance-time">{T.aboutMandre.dist3Time}</div>
          <h4>{T.aboutMandre.dist3Title}</h4>
          <p>{T.aboutMandre.dist3Body}</p>
        </div>
        <div class="distance-card">
          <div class="distance-time">{T.aboutMandre.dist4Time}</div>
          <h4>{T.aboutMandre.dist4Title}</h4>
          <p>{T.aboutMandre.dist4Body}</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ═══ MAP ═══ -->
  <section class="section-sm" style="background:var(--warm-sand);">
    <div class="container">
      <div class="text-center" style="margin-bottom:1.5rem;">
        <span class="section-label">{T.aboutMandre.findUsLabel}</span>
        <h2>{T.aboutMandre.findUsTitle}</h2>
      </div>
      <div class="map-placeholder">
        <iframe
          src="https://maps.google.com/maps?q=Mandre,+Island+of+Pag,+Croatia&z=14&output=embed"
          width="100%"
          height="320"
          style="border:0; border-radius:var(--radius-lg); display:block;"
          allowfullscreen
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Mandre, Island of Pag, Croatia"
        ></iframe>
        <div class="map-placeholder__actions">
          <a href="https://www.google.com/maps/dir/?api=1&destination=Blue+Moon+Apartment&destination_place_id=ChIJjWwKavA3YkcRtIxHt59t0pQ" target="_blank" rel="noopener" class="btn btn-primary">{T.directions.navigateGoogleMaps}</a>
          <a href={`${prefix}/directions`} class="btn btn-secondary">{T.aboutMandre.fullDirections}</a>
        </div>
      </div>
    </div>
  </section>

</PageLayout>

<style>
  .mandre-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .mandre-grid--reverse > *:first-child { order: 2; }
  .mandre-grid--reverse > *:last-child  { order: 1; }

  h2 { margin-bottom: 1rem; }
  p  { color: var(--text-muted); }

  .beach-tag {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--ocean-blue);
    color: white;
    font-size: 0.82rem;
    font-weight: 800;
    padding: 0.4rem 1rem;
    border-radius: 50px;
    margin-top: 1.25rem;
  }

  /* Family list */
  .mandre-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1.25rem;
  }
  .mandre-list li {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-size: 0.92rem;
    color: var(--text-dark);
  }
  .mandre-list__icon {
    font-size: 1.3rem;
    flex-shrink: 0;
    margin-top: 1px;
  }
  .mandre-list :global(strong) { color: var(--ocean-blue); }

  /* Convenience */
  .convenience-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }
  .convenience-item {
    background: var(--white);
    border-radius: var(--radius);
    padding: 1.5rem;
    box-shadow: var(--shadow-xs);
    text-align: center;
  }
  .convenience-icon { font-size: 2rem; display: block; margin-bottom: 0.75rem; }
  .convenience-item h4 { font-family:'Nunito',sans-serif; font-size:0.875rem; font-weight:800; color:var(--ocean-blue); margin-bottom:0.5rem; }
  .convenience-item p  { font-size:0.85rem; color:var(--text-muted); }

  /* Food cards */
  .food-card {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    background: var(--soft-sky);
    border-radius: var(--radius);
    padding: 1.25rem;
  }
  .food-card__emoji { font-size: 2.5rem; flex-shrink: 0; }
  .food-card h4 { font-family:'Nunito',sans-serif; font-size:1rem; font-weight:800; color:var(--ocean-blue); margin-bottom:0.35rem; }
  .food-card p  { font-size:0.88rem; color:var(--text-muted); }

  /* Our picks */
  .picks-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1.25rem;
  }
  .pick-card { padding: 1.5rem; }
  .pick-num {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    font-weight: 700;
    color: rgba(74,159,212,0.25);
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  .pick-card h4 { font-family:'Nunito',sans-serif; font-size:0.9rem; font-weight:800; color:var(--ocean-blue); margin-bottom:0.5rem; }
  .pick-card p  { font-size:0.82rem; color:var(--text-muted); font-style:italic; }

  /* Day trips */
  .trip-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1.25rem;
  }
  .trip-list li {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    padding: 0.75rem 1rem;
    background: var(--soft-sky);
    border-radius: var(--radius);
    border-left: 3px solid var(--sky-blue);
  }
  .trip-list strong { font-size:0.9rem; color: var(--ocean-blue); }
  .trip-list span   { font-size:0.85rem; color: var(--text-muted); }

  /* Distances */
  .distances-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }
  .distance-card {
    background: var(--white);
    border-radius: var(--radius);
    padding: 1.5rem;
    box-shadow: var(--shadow-xs);
  }
  .distance-time {
    font-family: 'Playfair Display', serif;
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--sky-blue);
    margin-bottom: 0.5rem;
  }
  .distance-card h4 { font-family:'Nunito',sans-serif; font-size:0.9rem; font-weight:800; color:var(--ocean-blue); margin-bottom:0.4rem; }
  .distance-card p  { font-size:0.83rem; color:var(--text-muted); }

  /* Map */
  .map-placeholder__actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-top: 1.5rem;
    flex-wrap: wrap;
  }

  @media (max-width: 1050px) {
    .picks-grid { grid-template-columns: repeat(3, 1fr); }
  }
  @media (max-width: 860px) {
    .mandre-grid { grid-template-columns: 1fr; gap: 2rem; }
    .convenience-grid { grid-template-columns: 1fr 1fr; }
    .distances-grid   { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 600px) {
    .convenience-grid { grid-template-columns: 1fr; }
    .distances-grid   { grid-template-columns: 1fr; }
    .picks-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 440px) {
    .picks-grid { grid-template-columns: 1fr; }
  }

  .carousel {
    position: relative;
    border-radius: var(--radius);
    overflow: hidden;
    aspect-ratio: 4/3;
    width: 100%;
  }
  .carousel__track {
    display: flex;
    height: 100%;
    transition: transform 0.4s ease;
  }
  .carousel__slide {
    min-width: 100%;
    height: 100%;
  }
  .carousel__slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  .carousel__btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.85);
    border: none;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
    transition: background var(--transition);
    color: var(--ocean-blue);
  }
  .carousel__btn:hover { background: white; }
  .carousel__btn--prev { left: 0.75rem; }
  .carousel__btn--next { right: 0.75rem; }
  .carousel__dots {
    position: absolute;
    bottom: 0.75rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 0.5rem;
    z-index: 2;
  }
  .carousel__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    border: none;
    cursor: pointer;
    padding: 0;
    transition: background var(--transition);
  }
  .carousel__dot--active { background: white; }
  .carousel__caption {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 0.5rem 1rem;
    background: rgba(0, 0, 0, 0.45);
    color: white;
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    text-align: center;
    z-index: 2;
  }
</style>

<script>
  document.querySelectorAll<HTMLElement>('.carousel').forEach(carousel => {
    const track = carousel.querySelector('.carousel__track') as HTMLElement;
    const dots = carousel.querySelectorAll('.carousel__dot');
    const prevBtn = carousel.querySelector('.carousel__btn--prev');
    const nextBtn = carousel.querySelector('.carousel__btn--next');
    const total = dots.length;
    let current = 0;

    const slides = carousel.querySelectorAll<HTMLElement>('.carousel__slide');
    const caption = carousel.querySelector<HTMLElement>('.carousel__caption');

    function goTo(index: number) {
      current = ((index % total) + total) % total;
      track.style.transform = `translateX(-${current * 100}%)`;
      dots.forEach((dot, i) => dot.classList.toggle('carousel__dot--active', i === current));
      if (caption) caption.textContent = slides[current]?.dataset.caption ?? '';
    }

    prevBtn?.addEventListener('click', () => goTo(current - 1));
    nextBtn?.addEventListener('click', () => goTo(current + 1));
    dots.forEach((dot, i) => dot.addEventListener('click', () => goTo(i)));

    let touchStartX = 0;
    carousel.addEventListener('touchstart', (e: TouchEvent) => { touchStartX = e.touches[0].clientX; }, { passive: true });
    carousel.addEventListener('touchend', (e: TouchEvent) => {
      const dx = e.changedTouches[0].clientX - touchStartX;
      if (Math.abs(dx) > 40) goTo(dx < 0 ? current + 1 : current - 1);
    });
  });
</script>
